manifest {
    name            = 'seirana/WESscDRS'
    mainScript      = 'main.nf'
    defaultBranch   = 'main'
    nextflowVersion = '!>=25.04.0'
}

/*
 * Default parameters.
 * Users can override with --input and --outdir.
 * baseDir is the root of this repo (where main.nf lives).
 */
params {
    input  = "${baseDir}/data"
    outdir = "${baseDir}/results"
}

process {
    cpus   = 4
    memory = '16 GB'
    time   = '24h'
}

/*
 * Profiles
 */
profiles {

    standard {
        process.executor = 'local'
    }

    apptainer {
        process.executor   = 'local'
        apptainer.enabled  = true
        /*
         * Path to your Singularity/Apptainer image inside the repo.
         * Nextflow will bind the repo into the container.
         */
        process.container  = "${baseDir}/containers/wes-scdrs.sif"
    }
}
